#include <stdio.h>
#include <string.h>
#include <ctype.h>

//liet ke so chia het cho 5
// co bao nhieu so

int main(){
	int a[100];
	int n, i;
	scanf("%d", &n);
	//input
	for (i = 0; i < n; i++){
		scanf("%d",*(a+i));
	}
	
	for (i = 0; i < n; i++){
	int count = 0
		for (j = i; j < n){
		if (a[i] % 5 == 0) 
			if (a[i]= a[j]) count++;
		}
	}
		
		
			
		 
	}
	
}
